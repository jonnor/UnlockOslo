- hosts: 
    - dlock
    - dlock-dev
  roles:
    - network
    - lusers
    - superusers
    - role: dlock-gateway
      tags: dlock-gateway
    - ssh-call-home-server
    - role: mqtt-server
      tags: mqtt-server
  tasks:
  - name: Install packages
    apt:
      name: "{{ item }}"
      install_recommends: no
    with_items:
      - vim-nox
      - aptitude
      - ansible
